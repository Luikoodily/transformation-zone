// DUMMY DATA — no real transformation photography or client consent exists yet.
// Do not present these as real results. Replace before launch.
export type Result = {
  duration: string;
  goal: string;
  client: string;
};

export const result: Result = {
  duration: "16 Weeks",
  goal: "Strength + Fat Loss",
  client: "Kojo A.",
};
